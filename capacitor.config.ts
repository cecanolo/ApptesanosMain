import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'Dev.YourS4nty.Apptesanos',
  appName: 'ApptesanosBase',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
